import 'home_failure.dart';

class BookSuggestionFailure extends HomeFailure {
  const BookSuggestionFailure({
    String? message,
    StackTrace? stackTrace,
  }) : super(
          message: message,
          stackTrace: stackTrace,
        );
}

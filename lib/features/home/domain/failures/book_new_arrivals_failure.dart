import 'home_failure.dart';

class BookNewArrivalsFailure extends HomeFailure {
  const BookNewArrivalsFailure({
    String? message,
    StackTrace? stackTrace,
  }) : super(
          message: message,
          stackTrace: stackTrace,
        );
}
